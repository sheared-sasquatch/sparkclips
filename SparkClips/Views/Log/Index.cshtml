@model IEnumerable<SparkClips.Models.HairyDatabase.LogEntry>
@{
    ViewData["Title"] = "Log";
}
@section Styles {
    <link rel="stylesheet" href="~/css/log.css" />
}
<h2>@ViewData["Title"].</h2>

<div class="row">
    <div class="col-md-10">
        <h3 class="pageTitle">Log</h3>
        <div class="flex-row">

        @*    @foreach (var item in Model)
            {
                <div class="flex-column card" style="width: 20rem;">
                    <img class="card-img-top" src="@Html.DisplayFor(x => item.Thumbnail)" alt="@Html.DisplayFor(model => item.DateTimeCreated)">
                    <div class="card-block">
                        <h4 class="card-title">@Html.DisplayFor(model => item.Location)@Html.DisplayFor(model => item.Barbers)@Html.DisplayFor(model => item.Cost)</h4>
                        <p class="card-text">@Html.DisplayFor(model => item.Description)</p>
                        <a asp-action="Detail" asp-route-id="@item.LogEntryID" class="btn btn-primary">View</a>
                    </div>
                </div>
            } *@

            <div>
                <a asp-action="Entry" class="btn btn-primary">Add a New Entry</a>
            </div>

            <div class="flex-column card" style="width: 20rem;">
                 <img class="card-img-top" src="http://www.jeremyvarner.com/blog/wp-content/uploads/2014/02/Harry-and-the-Hendersons.jpg" alt="Card image cap">
                 <div class="card-block">
                     <h4 class="card-title">Date of Haircut (From DateTimeCreated column)</h4>
                     <p class="card-text">Description user input</p>
                     <a asp-action="Detail" asp-route-id="" class="btn btn-primary">Go To Detail</a>
                 </div>
           </div>

        </div>
    </div>
</div>

@*<div class="list-group">
    <form method="post" enctype="multipart/form-data" asp-controller="Log" asp-action="UploadImage">
        <div class="form-group">
            <div class="col-md-10">
                <p>Upload one or more files using this form:</p>
                <input type="file" name="files" multiple />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <p>Location:</p>
                <input asp-for="Location" /> <br />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <p>Description:</p>
                <input asp-for="Description" /> <br />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <p>Cost:</p>
                <input asp-for="Cost" /> <br />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <p>Timestamp:</p>
                <input asp-for="Timestamp" /> <br />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <p>Barber/s:</p>
                <input asp-for="Barbers" /> <br />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                <input type="submit" value="Upload" />
            </div>
        </div>
    </form>
</div>*@

